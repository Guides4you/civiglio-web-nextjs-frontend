import React from 'react';
import { Alert } from 'antd';

// Placeholder semplificato per ThemeConfigurator
// TODO: Migrare la versione completa con ColorPicker e react-css-theme-switcher
const ThemeConfigurator = () => {
  return (
    <div>
      <Alert
        message="Theme Configuration"
        description="Theme configurator will be migrated in a future step. For now, default theme settings are applied."
        type="info"
        showIcon
      />
    </div>
  );
};

export default ThemeConfigurator;
